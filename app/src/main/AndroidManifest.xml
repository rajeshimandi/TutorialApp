<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anuraj.tutorialsapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".activities.TutorialsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.lifecycle.LifeCycleActivity" />
        <activity
            android:name=".activities.uiwidgets.UIWidgetsActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.intents.ActivityA" />
        <activity android:name=".activities.intents.ActivityB" />
        <activity android:name=".activities.fragments.FragmentActivity" />
        <activity android:name=".activities.sharedprefs.SharedPrefActivity" />
        <activity android:name=".activities.fragments.ViewPagerActivity" />
        <activity android:name=".activities.retrofit.RetrofitActivity" />
        <activity android:name=".activities.async.AyncTaskActivity" />
        <activity android:name=".activities.menu.MenuActivity" />
        <activity android:name=".activities.webview.WebViewActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.googlemap.MapsActivity"
            android:label="@string/title_activity_maps" />

        <service
            android:name=".activities.service.LocationTrackerService"
            android:exported="false" />

    </application>

</manifest>